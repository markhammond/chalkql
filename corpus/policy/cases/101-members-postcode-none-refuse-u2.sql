-- §3.11 under Refuse: the setting is about what a STAR surfaces, so a named column is still a
-- placeholder. See README §5 uncertainty U8 -- the section can also be read as refusing here.
SELECT postcode FROM members ORDER BY id
