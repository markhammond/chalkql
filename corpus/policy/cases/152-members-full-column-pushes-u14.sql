-- §3.8: a principal who sees the column in full loses nothing -- the expression pushes, because
-- the gate keys on the folded outcome and not on the node.
SELECT id FROM members WHERE UPPER(first_name) = 'TOMAS' ORDER BY id
