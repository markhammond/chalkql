-- §2, §5 (D204): the tenancy reached through the path member.org, expanded by the host into a
-- context list of member ids instead of read off a denormalised column. Same rows, other spelling.
SELECT id FROM orders ORDER BY id
