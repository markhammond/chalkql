-- §3.4, the remedy the design names: with the tenancy fixed by the statement the folded
-- disclosure is constant FULL and the same principal is answered.
SELECT id, amount FROM orders WHERE org_id = 1 ORDER BY id
