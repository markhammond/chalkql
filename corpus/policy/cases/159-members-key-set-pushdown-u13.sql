-- §2 item 2 and §3.7: a list above the fold ceiling is pushed as M5's key set, in calls sized by
-- the source's ceiling. See README §5 uncertainty U9 on what the flag should then say.
SELECT id FROM members ORDER BY id
