-- §8 corpus 17, D161: `default_disclosure = NONE` with `postcode` undeclared, under the default
-- UndisclosedColumns = Placeholder: the column stays in the row shape as a typed NULL.
SELECT * FROM members ORDER BY id
