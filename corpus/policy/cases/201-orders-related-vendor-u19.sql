-- D265 §0, §4. A row visible through the rows that belong to it: an order this principal reaches by
-- no tenancy of its own, and by one line of it that carries one of its own items.
SELECT id, org_id FROM orders ORDER BY id
